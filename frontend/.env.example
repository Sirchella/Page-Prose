# Backend API URL
# Local development (default — no need to set this locally)
# VITE_API_URL=http://localhost:8000/api

# Production — set this to your live backend URL when deploying
# VITE_API_URL=https://your-backend.railway.app/api
